{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug cui_app",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/cui_app",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Cargo Build (Debug)"
        }
    ]
}
