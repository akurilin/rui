# Rust build output
target/
build/

# Nested crate lockfile (workspace lockfile is tracked at /Cargo.lock)
cui_app/Cargo.lock

# App window capture helper logs
*.capture.log

# macOS Finder metadata
.DS_Store
**/.DS_Store
